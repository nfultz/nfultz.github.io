Use <code>rstudio-server</code> to manage an instance:

$ sudo rstudio-server restart

You can only have one R session per account. 

The easiest work around that I know of is to just create multiple users.

